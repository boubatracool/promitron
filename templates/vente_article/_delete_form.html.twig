<form method="post" action="{{ path('app_vente_article_delete', {'id': vente_article.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vente_article.id) }}">
    <button class="btn">Delete</button>
</form>
